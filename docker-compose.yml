version: "3.9"

services:
  sunrise-cms:
    image: ghcr.io/sgraham3/sunrise-cms:latest
    container_name: sunrise-cms

    ports:
      - "9080:9000"

    environment:
      NODE_ENV: production
      PORT: 8080
      LOGIN_USERNAME: admin
      LOGIN_PASSWORD: admin

    volumes:
      - ./data:/app/data
      - ./database:/app/database

    restart: unless-stopped
